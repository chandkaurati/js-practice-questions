

function init(){
    let name = "fireFox"
    function displayname(){
        console.log(name)
    }

    displayname(name)
}

// init()

// lexical scope means that in nested functions we can access 
// variables defiened insdie the outer/parent function 
// but we cannot accces variables defined in the nested function
// indside the parent functiono

// closures means if we are returning a funcntion"s refrence 
// it will not only returs that function but also it returs whole lexical scope
// of outer function means we can still have acces of variables defiend
// inside the outer function 


// Practical 



